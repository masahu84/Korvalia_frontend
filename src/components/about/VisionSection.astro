---
// Sección "La visión" con texto centrado - Contenido dinámico desde el backend
const API_BASE = import.meta.env.PUBLIC_API_URL || "http://localhost:4000";

// Valores por defecto
let visionTitle = "La visión";
let visionSubtitle = "La persona detrás de Korvalia";
let content = `Para Merche, una inmobiliaria debe ofrecer transparencia, soluciones y una comunicación directa. Ésta es la filosofía que mueve el proyecto.

Después de trabajar para varias agencias a nivel local y nacional durante más de 20 años, sé lo que representa el compromiso personal. La transparencia y la manera de entender el sector están basadas en principios sólidos, claridad, trato humano y un compromiso constante con las necesidades reales del cliente.`;

// Cargar datos del backend
try {
  const response = await fetch(`${API_BASE}/api/pages/about`);
  if (response.ok) {
    const result = await response.json();
    const blocks = result.data?.blocks || {};

    if (blocks.vision) {
      visionTitle = blocks.vision.title || visionTitle;
      visionSubtitle = blocks.vision.subtitle || visionSubtitle;
      content = blocks.vision.content || content;
    }
  }
} catch (error) {
  console.error("Error al cargar datos de La Visión:", error);
}
---

<section class="vision-section">
  <div class="container">
    <h2 class="section-title">{visionTitle}</h2>
    <p class="section-subtitle">{visionSubtitle}</p>

    <div class="vision-content" set:html={content.replace(/\n/g, '<br />')} />
  </div>
</section>

<style>
  .vision-section {
    padding: 4rem 0;
    background: white;
  }

  .container {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 1.5rem;
    text-align: center;
  }

  .section-title {
    font-family: "Montserrat", system-ui, sans-serif;
    font-size: 2rem;
    font-weight: 600;
    color: #39505d;
    margin: 0 0 0.5rem 0;
  }

  .section-subtitle {
    font-size: 1rem;
    color: #6a8074;
    margin: 0 0 2rem 0;
  }

  .vision-content {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .vision-content p {
    font-size: 0.9375rem;
    line-height: 1.8;
    color: #6a8074;
    margin: 0;
  }

  @media (max-width: 768px) {
    .vision-section {
      padding: 3rem 0;
    }

    .section-title {
      font-size: 1.75rem;
    }

    .vision-content p {
      font-size: 0.875rem;
    }
  }
</style>
